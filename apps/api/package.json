{
  "name": "api",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "db:migrate": "wrangler d1 migrations apply kotemon-jastip-db",
    "db:migrate:local": "wrangler d1 migrations apply kotemon-jastip-db --local",
    "db:wipe:local": "wrangler d1 execute kotemon-jastip-db --file=./scripts/wipe_users.sql --local"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.1.11",
    "hono": "^3.12.0",
    "jose": "^6.1.3",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.12.8",
    "@cloudflare/workers-types": "^4.20231218.0",
    "typescript": "^5.3.0",
    "vitest": "^4.0.18",
    "wrangler": "^4.61.1"
  }
}